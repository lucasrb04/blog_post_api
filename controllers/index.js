const userController = require('./User');
const categoryController = require('./Category');
const postController = require('./Post');

module.exports = { userController, categoryController, postController };